/**
 * The poster's design space — a fixed portrait canvas independent of the
 * browser viewport. Every layout coordinate lives here; preview and export
 * scale this space to their canvas.
 */
export const POSTER_W = 1080;
export const POSTER_H = 1350;
export const POSTER_RATIO = POSTER_W / POSTER_H;
